# Base de donnees IFVM

Ce dossier centralise la partie persistence du projet.

## Contenu

- `sql/` : schema SQL principal et scripts d'initialisation.
- `migrations/` : evolutions de structure si besoin.
- `seeds/` : donnees de test.

